# NEXORA — ფინალური პროექტის პრეზენტაციის სცენარი

ეს არის სასაუბრო მასალა პრეზენტაციისთვის. ფორმულირებები ეყრდნობა მიმდინარე კოდს და დოკუმენტაციას; არ უნდა ითქვას, რომ რეალური გადახდის გეითვეი ან ყველა პროდუქტის AI-გენერირებული სურათი უკვე დასრულებულია, თუ დემოში ეს ფაქტობრივად არ ჩანს.

---

## სლაიდი 1 — რა არის NEXORA

**სათაური:** NEXORA — მრავალენოვანი ტექნიკის ონლაინ-მაღაზია

**რა ვთქვა:**

NEXORA არის Django-ზე აგებული ტექნიკის ონლაინ-მაღაზია. მიზანი იყო არა მხოლოდ ვიზუალური კატალოგის შექმნა, არამედ ისეთი პლატფორმის აწყობა, სადაც რეალურად მუშაობს პროდუქტი, ძებნა, კალათა, შეკვეთა, მომხმარებლის ანგარიში, უსაფრთხოება, ადმინისტრირება და სამენოვანი გამოცდილება. პროექტი მხარს უჭერს ქართულ, ინგლისურ და რუსულ ენებს.

**დემო:** გახსენით მთავარი გვერდი → Shop → კონკრეტული პროდუქტი → კალათა.

---

## სლაიდი 2 — რატომ Django

**რა ვთქვა:**

Django ავირჩიეთ იმიტომ, რომ ერთი ჩარჩო გვაძლევს ORM-ს, authentication-ს, ფორმების ვალიდაციას, CSRF დაცვას, ადმინისტრაციულ პანელს, migrations-სა და template სისტემას. ამით ბიზნეს-ლოგიკა არ არის გაბნეული JavaScript-ში ან დაუცველ SQL სტრიქონებში.

**მთავარი ფაილები:** `musea/settings.py`, `store/models.py`, `store/views.py`, `store/admin.py`.

---

## სლაიდი 3 — არქიტექტურა: Django MTV და OOP

**რა ვთქვა:**

Django ხშირად აღწერილია როგორც MTV არქიტექტურა:

- **Model** — მონაცემთა ობიექტები და წესები: `Product`, `Category`, `ProductVariant`, `Order`, `Cart`, `Coupon`, `EmailVerification` და სხვა `store/models.py`-ში.
- **Template** — მომხმარებლის ინტერფეისი `templates/` საქაღალდეში.
- **View** — HTTP მოთხოვნის დამუშავება `store/views.py`-ში.

OOP აქ გამოიყენება რეალური პასუხისმგებლობების გასაყოფად: მაგალითად `ProductQuerySet` ინახავს კატალოგის გამოქვეყნებისა და search-ის ლოგიკას, `CartLine` არის კალათის სტრუქტურული ობიექტი, ხოლო სერვისები გამოყოფენ checkout-ის ტრანზაქციულ წესებს view-ებისგან.

**ჟიურისთვის მოკლე პასუხი:** OOP გვიცავს კოდის დუბლირებისგან: მონაცემი, ვალიდაცია და ბიზნეს-წესი ერთ ადგილასაა და მისი ტესტირება დამოუკიდებლად შეიძლება.

---

## სლაიდი 4 — მონაცემთა მოდელი

**რა ვთქვა:**

კატალოგის ბირთვია `Product`. მას უკავშირდება ბრენდი, კატეგორია, მედია-გალერეა, ვარიანტები, რეიტინგები, მიმოხილვები და SEO ველები. მრავალენოვანი დასახელებები და აღწერები ინახება ცალკე ველებში (`*_ka`, `*_en`, `*_ru`) და არა ავტომატურ თარგმანზე დამოკიდებული ტექსტით. ეს მნიშვნელოვანია, რადგან კონტროლირებადია როგორც ენა, ისე პროდუქტის ტექსტი.

**მნიშვნელოვანი წესი:** storefront-ზე `ProductQuerySet.published()` აჩვენებს მხოლოდ აქტიურ, გამოქვეყნებულ პროდუქტს, რომელსაც verified local primary image აქვს. ეს იცავს გვერდს ცარიელი ან არადასტურებული მედიისგან.

---

## სლაიდი 5 — კატალოგი, ძებნა და pagination

**რა ვთქვა:**

Shop გვერდს აქვს კატეგორიული ფილტრი, ფასის/რეიტინგის სორტირება, ძებნა და pagination. `Paginator` იყენებს `get_elided_page_range(on_each_side=2, on_ends=2)` მეთოდს, ამიტომ ათასობით გვერდისას ყველა ნომერი ერთდროულად არ ჩნდება. ფილტრის, ძიების და სორტირების query პარამეტრები გვერდის შეცვლისას შენარჩუნდება reusable template-ით `templates/store/pagination.html`.

PostgreSQL-ზე search იყენებს `SearchVector`, `SearchRank` და trigram similarity-ს; SQLite-ისთვის დარჩენილია portable `icontains` fallback. ეს ნიშნავს, რომ დემოც მუშაობს, ხოლო production PostgreSQL გარემო უკეთეს ძებნას იღებს.

---

## სლაიდი 6 — პროდუქტის გვერდი და UX

**რა ვთქვა:**

პროდუქტის გვერდზე არის verified მედიის გალერეა, ვარიანტები, ფასი, მარაგი, აღწერა, ტექნიკური მახასიათებლები, რეიტინგი და კალათაში დამატება. ვარიანტების არჩევა გავლენას ახდენს ეფექტურ ფასსა და მარაგზე. მომხმარებელს შეუძლია wishlist, compare და review/rating ფუნქციების გამოყენება.

მთავარი გვერდი იყენებს ლოკალურ Three.js/WebGL გამოცდილებას, ადგილობრივ აუდიო ტრეკებს და მიკროფონს მხოლოდ მომხმარებლის ინიციატივით. ამ ფუნქციის მიზანია ბრენდის ეფექტი; შესყიდვის კრიტიკული ნაწილები მაინც სტანდარტულ, სწრაფ და ხელმისაწვდომ HTML გამოცდილებაზეა.

---

## სლაიდი 7 — კალათა და checkout

**რა ვთქვა:**

კალათა მუშაობს სტუმრისთვის session-ზე, ხოლო ავტორიზებული მომხმარებლისთვის მუდმივ `Cart`/`CartItem` მოდელებზე. login-ის შემდეგ Django signal ავტომატურად აერთიანებს სტუმრის კალათას მომხმარებლის კალათასთან (`store/cart_signals.py`).

Checkout-ის დროს სერვისის ფენა იყენებს `transaction.atomic()` და `select_for_update()`. ეს ნიშნავს, რომ ბოლო ერთეულის პარალელური შეკვეთისას მარაგი იკეტება ტრანზაქციის დასრულებამდე და overselling-ის რისკი მცირდება.

**მნიშვნელოვანი შეზღუდვა:** მიმდინარე checkout არის დემო/შეკვეთის შექმნის flow; რეალური ბარათის გადახდის პროვაიდერი განზრახ არ არის მიერთებული.

---

## სლაიდი 8 — ავტორიზაცია და ელფოსტის დადასტურება

**რა ვთქვა:**

რეგისტრაციის შემდეგ ანგარიში არ აქტიურდება მანამდე, სანამ მომხმარებელი თავის ელფოსტას არ დაადასტურებს. მიმდინარე უსაფრთხო მექანიზმი ქმნის ერთჯერად ბმულს, რომელსაც დროის ლიმიტი აქვს. token მონაცემთა ბაზაში plaintext-ად არ ინახება — გამოიყენება Django-ის password hashing.

პაროლის აღდგენა სხვა flow-ა: მომხმარებელი იღებს ექვსნიშნა ერთჯერად კოდს, რომელსაც ასევე აქვს ვადა, მცდელობების ლიმიტი და resend cooldown. ორი flow განზრახ განსხვავდება: signup ადასტურებს ელფოსტის ფლობას ბმულით; password reset ითხოვს მომხმარებლისგან კოდის შეყვანას.

**დემო:** Signup → ელფოსტის ბმული → account activation; შემდეგ Password reset → code → ახალი პაროლი.

---

## სლაიდი 9 — უსაფრთხოება

**რა ვთქვა:**

უსაფრთხოება არაა ერთი ფუნქცია — ის რამდენიმე ფენითაა აწყობილი:

- `DJANGO_SECRET_KEY` სავალდებულოა; production-ში აპლიკაცია არ ეშვება მის გარეშე.
- `DEBUG` ნაგულისხმევად გამორთულია.
- HTTPS cookies, HSTS, CSRF, clickjacking და MIME-sniffing დაცვა ჩართულია production რეჟიმში.
- `SecurityHeadersMiddleware` ამატებს CSP-სა და Permissions-Policy-ს.
- Login, admin login, signup, verification recovery და password reset rate-limit-დება shared cache-ით IP-სა და account identifier-ზე.
- `request_ip()` X-Forwarded-For-ს ენდობა მხოლოდ წინასწარ მითითებული trusted proxy allowlist-იდან.
- შეკვეთის გვერდი ამოწმებს მფლობელობას ან სტუმრის session-ში არსებულ order reference-ს.

**სათქმელი:** მიზანი იყო “security by default” — ანუ შეცდომით დაუცველი production კონფიგურაცია არ მივიღოთ.

---

## სლაიდი 10 — Signals, middleware და forms

**რა ვთქვა:**

პროექტში გვაქვს სამი Django მექანიზმი, რომლებიც ხშირად ეკითხებათ:

- **Forms** (`store/forms.py`) — input-ის ვალიდაცია, password rules, review და checkout მონაცემების უსაფრთხო დამუშავება.
- **Signals** — login-ზე სტუმრის კალათის გაერთიანება; review/rating ცვლილებისას პროდუქტის საშუალო რეიტინგისა და რაოდენობის სინქრონიზაცია (`store/cart_signals.py`, `store/rating_signals.py`).
- **Middleware** — ყველა მოთხოვნაზე security headers-ის დამატება და admin login brute-force დაცვა (`store/middleware.py`).

ეს მიდგომა view-ებს ამცირებს: გვერდი მართავს HTTP პასუხს, ხოლო განმეორებადი წესები სწორ ფენაშია.

---

## სლაიდი 11 — სამენოვანი გამოცდილება და accessibility

**რა ვთქვა:**

Routes მუშაობს ქართულად `/`, ინგლისურად `/en/` და რუსულად `/ru/`. `LocaleMiddleware` არჩევს ენას, ხოლო `html lang` იცვლება მიმდინარე ენის მიხედვით. ქართული ტიპოგრაფიისთვის პროექტს აქვს ლოკალურად მიწოდებული FiraGO ფონტები და `static/georgian-type.css`.

Accessibility ნაწილში გამოყენებულია alt ტექსტები, label-ები, `aria-live`, `role=dialog`, skip-target, visible focus და `prefers-reduced-motion`. ანიმაციები არ უნდა გახდეს ბარიერი keyboard ან reduced-motion მომხმარებლებისთვის.

---

## სლაიდი 12 — AI shopping guide

**რა ვთქვა:**

Ask NEXORA არის server-side shopping guide. ის იღებს მხოლოდ შეზღუდულ კატალოგის კონტექსტს და ინსტრუქცია კრძალავს ფასის, მარაგის, გარანტიის ან პირადი ინფორმაციის გამოგონებას. მხარდაჭერილია Gemini ან Meta provider კონფიგურაციით; API key ინახება `.env`/deployment secrets-ში და არა Git-ში.

გარეთა API შეცდომისას არის მოკლე timeout, retry და circuit-breaker cache. ამით ერთ პრობლემურ provider-ს არ შეუძლია ყველა მომხმარებლის request-ის დაყოვნება.

**რეალისტური შეზღუდვა:** მაღალი დატვირთვისთვის AI და SMTP უკეთესია Redis/Celery background queue-ში გადავიდეს; მიმდინარე ვერსია უსაფრთხოდ ზღუდავს და აკონტროლებს მოთხოვნებს, მაგრამ ეს არის შემდეგი scale ეტაპი.

---

## სლაიდი 13 — Admin და ოპერაციული მართვა

**რა ვთქვა:**

Django admin არის შიდა ოპერაციული პანელი: პროდუქტები, კატეგორიები, ბრენდები, მედია, ვარიანტები, კუპონები, შეკვეთები და მომხმარებლის მონაცემები სამართავია ერთ ადგილას. კატალოგისთვის არის management commands — sync, gallery enrich/refresh, audit და rating repair.

მედიისთვის ინახება source URL, licence/attribution და verification metadata. ეს ეხმარება ადმინისტრატორს გაიგოს საიდან მოდის პროდუქტის სურათი და რატომ შეიძლება/არ შეიძლება მისი გამოქვეყნება.

---

## სლაიდი 14 — ტესტირება და CI

**რა ვთქვა:**

ტესტები ცალკე ფაილებად ამოწმებს commerce security-ს, email verification-ს, password reset-ს, pagination-ს, რეიტინგებს, კატალოგის წესებს, footer-ს, guide language-ს და production hardening-ს. GitHub Actions workflow (`.github/workflows/ci.yml`) ყოველ push/PR-ზე:

1. ტესტებს უშვებს PostgreSQL 17 და Redis 7.4 service-ებით;
2. ამოწმებს migrations-სა და dependency integrity-ს;
3. ცალკე production-like job-ში უშვებს `check --deploy` და `collectstatic`.

**თუ კითხვა იქნება GitHub Actions წითელ სტატუსზე:** workflow-ის კონფიგურაცია პროექტშია; GitHub-ის ეკრანზე ნაჩვენები კონკრეტული run შეიძლება ჩავარდეს ანგარიშის billing lock-ის გამო, ანუ job საერთოდ არ იწყება. ეს განსხვავდება კოდის test failure-ისგან.

---

## სლაიდი 15 — Deployment და ინფრასტრუქტურა

**რა ვთქვა:**

ლოკალურ დემოში შეიძლება SQLite გამოვიყენოთ. Production-ისთვის პროექტი გათვლილია PostgreSQL-ზე, Redis-ზე, WhiteNoise static-ზე და Docker Compose-ზე. `render.yaml`, `Dockerfile` და `docker-compose.yml` აღწერს ამ მიმართულებას; `/health/` endpoint აბრუნებს readiness მდგომარეობას database/cache-ისთვის დეტალების გამჟღავნების გარეშე.

**Vercel-ის შესახებ სწორი პასუხი:** Vercel იდეალურია static/Next.js პროექტისთვის, მაგრამ ეს არის მუდმივი Django + PostgreSQL + Redis აპლიკაცია. ტექნიკურად შესაძლებელია serverless ადაპტაცია, თუმცა ამ პროექტისთვის Render, Railway ან Docker-compatible პლატფორმა უფრო ბუნებრივი და საიმედო არჩევანია. დომენი მოგვიანებით შეიძლება მიეთითოს როგორც `nexora...`.

---

## სლაიდი 16 — ძლიერი მხარეები და შემდეგი ნაბიჯები

**ძლიერი მხარეები:**

- სრული Django e-commerce flow, არა მხოლოდ სტატიკური დიზაინი;
- სამენოვანი მონაცემები და UI;
- verified product-media publication წესები;
- ტრანზაქციული მარაგი და persistent/guest cart;
- auth, email verification, password reset და rate-limits;
- responsive დიზაინი, ადგილობრივი asset-ები და accessibility საფუძვლები;
- PostgreSQL/Redis/Docker/CI readiness.

**შემდეგი ეტაპი, პატიოსნად:**

1. რეალური payment provider-ის sandbox/webhook ინტეგრაცია;
2. SMTP და AI პასუხების ფონური queue (Celery/Redis) დიდი დატვირთვისთვის;
3. production მონიტორინგი, backup/restore drill და error tracking;
4. კატალოგის საბოლოო curated/AI-გენერირებული მედიის ხარისხის კონტროლი;
5. რეალურ მომხმარებლებზე visual/performance audit.

---

# რეკომენდებული ცოცხალი დემო — 4–5 წუთი

1. მთავარი გვერდი — ბრენდის ვიზუალი, ენის არჩევა, theme და motion.
2. Shop — ძიება, კატეგორია, სორტირება, pagination.
3. Product — გალერეა, ვარიანტი, რეიტინგი, wishlist/compare.
4. Cart/checkout — პროდუქტის დამატება, რაოდენობა, coupon, order creation.
5. Signup/email link ან password reset code — უსაფრთხო account flow.
6. Admin — ერთი პროდუქტის/მედიის/მარაგის მართვა.
7. ბოლოს აჩვენეთ GitHub, Docker/Render ფაილები და CI workflow.

---

# მოსალოდნელი კითხვები და მოკლე პასუხები

### 1. რატომ არ დაწერეთ raw SQL?

Django ORM გვაძლევს parameterized queries-ს, migrations-ს და model-level წესებს. საჭირო ადგილას PostgreSQL full-text search Django-ს ოფიციალური ORM API-ითაა დამატებული.

### 2. როგორ იცავთ მარაგს ორი ერთდროული მომხმარებლისგან?

Checkout service-ში transaction.atomic და select_for_update კეტავს შესაბამის პროდუქტსა და ვარიანტს ტრანზაქციისას; შემდეგ მოწმდება მარაგი და მხოლოდ ამის შემდეგ იწერება order/stock update.

### 3. რატომ არის password reset code, მაგრამ email verification link?

ორივე მიზნისთვის ყველაზე კომფორტული UX განსხვავებულია. signup-ზე ბმულზე დაჭერა ადასტურებს mailbox-ის ფლობას; პაროლის აღდგენაზე მომხმარებელი თავად შეიყვანს მოკლე, ვადიან კოდს. ორივე token hash-ად ინახება და ერთჯერადია.

### 4. სად ინახება API key და Gmail password?

გარემო ცვლადებში ან `.env`-ში, რომელიც `.gitignore`-შია. GitHub-ში არ ხვდება არც API key, არც SMTP password, არც SQLite database და არც `.venv`.

### 5. რა როლი აქვს Redis-ს?

Production-ში Redis არის საერთო cache და rate-limit counters-ის საცავი. ამის გარეშე რამდენიმე Gunicorn worker ერთმანეთისგან იზოლირებულ ლიმიტებს გამოიყენებდა.

### 6. როგორ არის დაცული rate-limit proxy-ის უკან?

კლიენტის IP `X-Forwarded-For`-დან იკითხება მხოლოდ მაშინ, როცა პირდაპირი proxy მისამართი allowlist-შია. სხვა შემთხვევაში გამოიყენება `REMOTE_ADDR`, რათა მომხმარებელმა spoofed header-ით ვერ მოატყუოს სისტემა.

### 7. რატომ გაქვთ signal-ები?

რადგან cross-cutting რეაქციებია: login-ზე session cart უნდა გაერთიანდეს; review/rating ცვლილებაზე aggregate უნდა განახლდეს. ეს view-ში რომ ჩაიწეროს, დუბლირება და გამოტოვების რისკი გაიზრდება.

### 8. თქვენი 3D/WebGL ნაწილი აუცილებელია?

არა, ყიდვის ლოგიკისთვის არაა აუცილებელი. ის ბრენდის/ემოციური გამოცდილების ფენაა. ამიტომ ძირითადი კატალოგი და checkout მის გარეშეც server-rendered და ხელმისაწვდომია.

### 9. რატომ არა Vercel?

Vercel შესაძლებელია ადაპტაციით, მაგრამ მუდმივი Django აპი PostgreSQL/Redis/SMTP ინტეგრაციით უფრო ბუნებრივად მუშაობს Render/Railway/Docker ჰოსტინგზე. ეს ტექნიკური არგუმენტია, არა დიზაინის არჩევანი.

### 10. რა აკლია ნამდვილ ონლაინ მაღაზიამდე?

სერტიფიცირებული გადახდის პროვაიდერი, webhook-ები, საგადასახადო/მიწოდების რეალური წესები, მონიტორინგი, backup პროცესი და load-testing. ეს დემოში მიზანმიმართულად არ არის მიბმული.

---

# პრეზენტაციის ერთი წინადადებით დასასრული

NEXORA არის Django-ზე აწყობილი, სამენოვანი ტექნიკის მაღაზიის სრულფასოვანი დემო, სადაც დიზაინთან ერთად რეალურად არის მოდელირებული კატალოგი, უსაფრთხო მომხმარებლის flow, ტრანზაქციული შეკვეთა, ადმინისტრირება და production-ready ინფრასტრუქტურის საფუძვლები.
